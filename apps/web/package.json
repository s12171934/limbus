{
  "name": "@limbus/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "check": "tsc -b",
    "test": "bun test --pass-with-no-tests",
    "deploy": "wrangler pages deploy dist --project-name limbus-deck"
  },
  "dependencies": {
    "@limbus/contracts": "workspace:*",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "vite": "8.1.4",
    "vite-plugin-pwa": "1.3.0",
    "wrangler": "4.110.0"
  }
}
